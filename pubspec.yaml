name: flutter_restful_api
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  bloc_concurrency: ^0.2.2
  flutter_bloc: ^8.1.3
  google_fonts: ^6.1.0
  dio: ^5.8.0+1
  equatable: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
